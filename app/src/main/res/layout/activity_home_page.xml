<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="16dp"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingTop="16dp"
    tools:context="com.example.apolcz.mysong.HomePageActivity">


    <Button
        android:id="@+id/newSong"
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:layout_marginBottom="20dp"
        android:text = "New Song"
       />
    <Button

        android:id="@+id/loadSong"
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:layout_below="@id/newSong"
        android:layout_marginBottom="20dp"
        android:layout_marginTop="20dp"
        android:text = "Load Song"
        />

    <Button
        android:id="@+id/showNotes"
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:layout_below="@id/loadSong"
        android:layout_marginBottom="20dp"
        android:layout_marginTop="20dp"
        android:text= "Notes"
        />
</RelativeLayout>
